import { Checkbox, extendVariants } from '@nextui-org/react';

const SeadaCheckbox = extendVariants(Checkbox, {
    defaultVariants: {
        radius: 'none',
    },
});

export default SeadaCheckbox;
