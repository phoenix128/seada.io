import { ISourceSetupCollection } from '@seada.io/core/interface/source-setup';

const getSourceSetups = (setups?: ISourceSetupCollection): ISourceSetupCollection => {
    return setups;
};

export default getSourceSetups;
