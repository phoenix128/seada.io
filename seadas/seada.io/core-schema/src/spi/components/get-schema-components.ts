import { ISchemaComponentsCollection } from '@seada.io/core-schema/spi/components/interface';

/**
 * Get schema component schema by type code
 * @param components
 */
const getSchemaComponents = (components?: ISchemaComponentsCollection): ISchemaComponentsCollection => {
    return components;
};

export default getSchemaComponents;
